# spacy-flask-web


----INSTALL----

pip install flask
pip instal flask-cors (for dev only)

spacy:
use conda (create venv, activate, and install conda-forge spacy)

-----RUNNING------
run server with:

flask --app flasky run

install spacy trained pipline with:

python -m spacy download en_core_web_sm


